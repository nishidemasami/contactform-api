# 00000001_コーディング規約と実装例

Rustの実装時のコーディング規約、アーキテクチャ方針、DDD実装方針を定義する。
また、Rustの実装例を列挙する。

## コーディング時のルール概要

- AI開発
  - AIの認識負荷を下げるため、 let 宣言にも型（Type Annotation）を省略せずに明記する

- アーキテクチャ
  - クリーンアーキテクチャとDDDを基本方針とする。
  - `domain` は他の業務層に依存してはならない。
  - `application` は `domain` にのみ依存する。
  - `infrastructure` は `domain` のリポジトリtraitを実装する。
  - `presentation` はHTTP、Lambda、OpenAPIなど外部入出力を担当する。
  - レイヤー間マッピングは、依存方向を崩さない位置に実装する。
  - 単純な構造変換は `From` トレイトで実装する。
  - バリデーション、ドメイン値オブジェクト生成、外部データからの復元など、失敗可能性がある変換は `TryFrom` トレイトで実装する。
  - ただし、RustのOrphan Ruleにより `From` / `TryFrom` トレイトで実装できない場合は、変換先または変換責務を持つ型の `impl` ブロックに、独自の名前付きメソッド（`from_xxx` / `to_xxx` / `try_from_xxx` / `try_to_xxx`）として実装する。
  - 具体的な配置方針は以下の通り。
    - `presentation request` → `application input` は `presentation` 側で変換する。
    - `application output` → `presentation response` は `presentation` 側で変換する。
    - `domain entity` → `SeaORM ActiveModel` は `infrastructure` 側で変換する。
    - `SeaORM Model` → `domain entity` は `infrastructure` 側で `TryFrom` または `try_from_model` により変換する。
    - `domain value object` の生成を伴う変換は `TryFrom` または `try_from_xxx` とする。
    - 単純なDTO変換は `From` または `from_xxx` とする。
  - 外側のレイヤーの型を内側のレイヤーに持ち込んではならない。
    - 例: `domain` は `presentation` のrequest/response型、SeaORMのModel/ActiveModel、Lambda/API Gateway型に依存してはならない。
    - 例: `application` はSeaORM、Lambda、HTTP、OpenAPIに依存してはならない。

- フォーマット
  - `rustfmt` を使用する。
  - 作業終了前に `cargo fmt -- --check` を実行する。

- 静的解析
  - `clippy` を使用する。
  - 作業終了前に以下を実行する。
    - `cargo clippy --workspace --all-targets --all-features -- -D warnings`

- コンパイルチェック
  - `cargo check --workspace --all-targets --all-features` を実行する。

- テスト
  - 通常テストは `cargo test --workspace --all-features` を実行する。
  - 重いテスト、プロパティベーステスト、ローカルDB必須テストは `#[ignore = "..."]` を付与する。
  - ignore付きテストを含めた確認時のみ `cargo test --workspace --all-features -- --include-ignored` を実行する。
  - バグ修正時には、再発防止および修正の検証を目的として、対象バグに対するテストケースを必ず追加・更新する。

- ドキュメント
  - public itemには原則として日本語のドキュメンテーションコメントを記載する。
  - ドキュメント出力は `cargo doc --workspace --no-deps` を使用する。
  - 内部実装もレビュー対象にする場合、 `--document-private-items` を使用する。

- エラー処理
  - domain層ではドメインルール違反を表すエラーを定義する。
  - application層ではユースケース単位のエラーに変換する。
  - presentation層ではアプリケーションエラーをHTTPステータスへ変換する。
  - infrastructure層の詳細な外部エラーをそのままAPIレスポンスへ返してはならない。

## ビジネスロジック

Rustのワークスペースにより循環参照を未然に防ぐことでクリーンアーキテクチャを実装しやすくする。

※以下の例は、重要な点以外はドキュメンテーションコメントなどを省略しているが、実際にはcargo docにより出力されてドキュメントに使用されるため、DDDに従い詳細に事細かにドキュメンテーションコメントを記入する。また、細かい実装を省略しているため、実装時は随時コンパイルエラーを修正する。

```text
core
├── domain
│   ├── Cargo.toml
│   └── src
│       └── inquiry
│           ├── entity.rs
│           ├── repository.rs
│           ├── service.rs
│           ├── value_object.rs
│           ├── domain_error.rs
│           └── repository_error.rs
│
├── application
│   ├── Cargo.toml
│   └── src
│       └── inquiry
│           ├── dto.rs
│           ├── create.rs
│           └── error.rs
│
├── infrastructure
│   ├── Cargo.toml
│   └── src
│       ├── db
│       │   ├── config.rs
│       │   ├── connection.rs
│       │   ├── aurora_dsql.rs
│       │   └── postgres.rs
│       │
│       └── repository
│           └── inquiry
│               └── seaorm_inquiry_repository.rs
│
└── presentation
    ├── Cargo.toml
    └── src
        ├── api
        └── lambda
```

## 実装例

### `Cargo.toml`

ルートディレクトリに、Rustのワークスペースの親となる`Cargo.toml`を作成する。

```toml
[profile.release]
lto = true
codegen-units = 1
opt-level = 3
panic = "abort"
strip = true

[workspace]
members = [
  "api/lambda",
  "core/application",
  "core/domain",
  "core/infrastructure",
  "core/presentation",
  "db/sea_orm_entities",
]
resolver = "3"

[workspace.package]
edition = "2024"

[workspace.dependencies]
async-trait = "0.1"
chrono = { version = "0.4", features = ["clock", "serde"] }
sea-orm = { version = "2", features = [
  "sqlx-postgres",
  "runtime-tokio-rustls",
  "macros",
  "with-chrono",
  "with-uuid",
] }
serde = { version = "1", features = ["derive"] }
serde_json = "1"
thiserror = "2"
tokio = { version = "1", features = ["macros", "rt"] }
uuid = { version = "1", features = ["serde", "v7"] }
anyhow = "1"
aurora-dsql-sqlx-connector = { version = "0.2", features = ["pool"] }
tracing = "0.1"
lambda_runtime = "0.13"
aws_lambda_events = { version = "0.15", default-features = false, features = ["apigw"] }
utoipa = { version = "4", features = ["uuid"] }

# テスト用ライブラリ
rstest = "0.26.0" 
```

### ドメイン層

#### `core/domain/src/inquiry/entity.rs`

```rust
pub struct Inquiry {
    id: InquiryId,
    name: InquiryName,
    email: InquiryEmail,
    message: InquiryMessage,
    created_at: DateTime<FixedOffset>,
}
```

#### `core/domain/src/inquiry/repository.rs`

```rust
use async_trait::async_trait;

use super::{
    entity::Inquiry,
    repository_error::InquiryRepositoryError,
};

#[async_trait]
pub trait InquiryRepository: Send + Sync {
    async fn save(
        &self,
        inquiry: Inquiry,
    ) -> Result<Inquiry, InquiryRepositoryError>;
}
```

#### `core/domain/src/inquiry/value_object.rs`

```rust
use std::fmt::Display;

use uuid::Uuid;

use super::domain_error::InquiryDomainError;

#[derive(Debug, Clone, PartialEq, Eq)]
pub struct InquiryEmail(String);

impl InquiryEmail {
    pub fn new(
        value: impl Into<String>,
    ) -> Result<Self, InquiryDomainError> {
        let value = value.into();

        if value.trim().is_empty() {
            return Err(InquiryDomainError::EmptyEmail);
        }
        
        if value.chars().count() > 256 {
            return Err(InquiryDomainError::InvalidEmail);
        }

        Ok(Self(value))
    }

    pub fn value(&self) -> &str {
        &self.0
    }
}

#[derive(Debug, Clone, PartialEq, Eq)]
pub struct InquiryName(String);

impl InquiryName {
    pub fn new(
        value: impl Into<String>,
    ) -> Result<Self, InquiryDomainError> {
        let value = value.into();

        if value.trim().is_empty() {
            return Err(InquiryDomainError::EmptyName);
        }

        if value.chars().count() > 100 {
            return Err(InquiryDomainError::NameTooLong);
        }

        Ok(Self(value))
    }

    pub fn value(&self) -> &str {
        &self.0
    }
}

#[derive(Debug, Clone, PartialEq, Eq)]
pub struct InquiryMessage(String);

impl InquiryMessage {
    pub fn new(
        value: impl Into<String>,
    ) -> Result<Self, InquiryDomainError> {
        let value = value.into();

        if value.trim().is_empty() {
            return Err(InquiryDomainError::EmptyMessage);
        }

        if value.chars().count() > 5000 {
            return Err(InquiryDomainError::MessageTooLong);
        }

        Ok(Self(value))
    }

    pub fn value(&self) -> &str {
        &self.0
    }
}

#[cfg(test)]
mod tests {
    use super::*;

    #[test]
    fn 何らかのテスト() {
        // 何らかのテストを実装
    }
}
```

#### `core/domain/src/inquiry/domain_error.rs`

```rust
use thiserror::Error;

#[derive(Debug, Error, Clone, PartialEq, Eq)]
pub enum InquiryDomainError {
    #[error("name is required")]
    EmptyName,

    #[error("name is too long")]
    NameTooLong,

    #[error("email is required")]
    EmptyEmail,

    #[error("invalid email")]
    InvalidEmail,

    #[error("message is required")]
    EmptyMessage,

    #[error("message is too long")]
    MessageTooLong,
}
```

#### `core/domain/src/inquiry/repository_error.rs`

```rust
use thiserror::Error;

#[derive(Debug, Error, Clone, PartialEq, Eq)]
pub enum InquiryRepositoryError {
    #[error("inquiry already exists")]
    Duplicate,

    #[error("inquiry not found")]
    NotFound,

    #[error("infrastructure error")]
    Infrastructure,

    #[error("stored inquiry data is corrupted")]
    CorruptedData,
}
```

### アプリケーション層

#### `core/application/src/inquiry/dto.rs`

```rust
pub struct CreateInquiryInput {
    pub name: String,
    pub email: String,
    pub message: String,
}
pub struct CreateInquiryOutput {
    pub id: Uuid,
}
```

#### `core/application/src/inquiry/create.rs`

```rust
pub struct CreateInquiryUsecase {
    repository: Arc<dyn InquiryRepository>,
}
impl CreateInquiryUsecase {
    pub fn new(
        repository: Arc<dyn InquiryRepository>,
    ) -> Self {
        Self { repository }
    }
    pub async fn execute(
        &self,
        input: CreateInquiryInput,
    ) -> Result<CreateInquiryOutput, CreateInquiryError> {
        let inquiry = Inquiry::new(
            InquiryId::new(Uuid::now_v7()),
            InquiryName::new(input.name)?,
            InquiryEmail::new(input.email)?,
            InquiryMessage::new(input.message)?,
            DateTime::<FixedOffset>::from(Utc::now()),
        );

        let saved =
            self.repository.save(inquiry).await?;

        Ok(CreateInquiryOutput { id: *saved.id().value() })
    }
}
```

#### `core/application/src/inquiry/error.rs`

```rust
use domain::inquiry::{
    domain_error::InquiryDomainError,
    repository_error::InquiryRepositoryError,
};

#[derive(Debug, thiserror::Error)]
pub enum CreateInquiryError {
    #[error(transparent)]
    Domain(#[from] InquiryDomainError),

    #[error(transparent)]
    Repository(#[from] InquiryRepositoryError),
}
```

### インフラストラクチャ層

#### `core/infrastructure/src/repository/inquiry/seaorm_inquiry_repository.rs`

```rust
pub struct SeaOrmInquiryRepository {
    db: DatabaseConnection,
}
#[async_trait]
impl InquiryRepository for SeaOrmInquiryRepository {
    async fn save(
        &self,
        inquiry: Inquiry,
    ) -> Result<Inquiry, InquiryRepositoryError> {
        let active_model = to_active_model(&inquiry);

        let result = Inquiry::insert(active_model)
            .exec_without_returning(&self.db)
            .await
            .map_err(|e| {
                tracing::error!("Database execution failed: {:?}", e);
                InquiryRepositoryError::Infrastructure
            })?;

        Ok(inquiry)
    }
}
```

#### `core/infrastructure/src/db/config.rs`

```rust
use std::borrow::Cow;

#[derive(Debug, Clone)]
pub enum DatabaseConfig {
    AuroraDSQL {
        endpoint: Cow<'static, str>,
        region: Cow<'static, str>,
        role: Cow<'static, str>,
    },

    PostgreSQL {
        host: Cow<'static, str>,
        port: u16,
        database: Cow<'static, str>,
        username: Cow<'static, str>,
        password: Cow<'static, str>,
    },
}
```

#### `core/infrastructure/src/db/connection.rs`

```rust
use sea_orm::sqlx::PgPool;
use sea_orm::{DatabaseConnection, SqlxPostgresConnector};

use super::{
    aurora_dsql,
    config::DatabaseConfig,
    postgres,
};

pub async fn create_connection(
    config: &DatabaseConfig,
) -> anyhow::Result<DatabaseConnection> {
    let pool = match config {
        DatabaseConfig::AuroraDSQL {
            endpoint,
            region,
            role,
        } => {
            aurora_dsql::connect(
                endpoint,
                region,
                role,
            )
            .await?
        }

        DatabaseConfig::PostgreSQL {
            host,
            port,
            database,
            username,
            password,
        } => {
            postgres::connect(
                host,
                *port,
                database,
                username,
                password,
            )
            .await?
        }
    };
    Ok(SqlxPostgresConnector::from_sqlx_postgres_pool(pool))
}
```

#### `core/infrastructure/src/db/aurora_dsql.rs`

```rust
use anyhow::Error;
use aurora_dsql_sqlx_connector::pool;
use sea_orm::sqlx::PgPool;

fn build_connection_string(role: &str, endpoint: &str, region: &str) -> String {
    format!("postgres://{role}@{endpoint}/postgres?region={region}")
}

pub async fn connect(
    endpoint: &str,
    region: &str,
    role: &str,
) -> Result<PgPool, Error> {
    tracing::info!("Creating database connection with Aurora DSQL SQLx connector...");
    let connection_string = build_connection_string(
        role,
        endpoint,
        region,
    );
    let pool = pool::connect(&connection_string)
        .await
        .map_err(|e| anyhow::anyhow!("Failed to connect to database: {}", e))?;

    Ok(pool)
}
```

#### `core/infrastructure/src/db/postgres.rs`

```rust
use anyhow::Error;
use sea_orm::sqlx::PgPool;

pub async fn connect(
    host: &str,
    port: u16,
    database: &str,
    username: &str,
    password: &str,
) -> Result<PgPool, Error> {
    let database_url = format!(
        "postgres://{}:{}@{}:{}/{}",
        username,
        password,
        host,
        port,
        database,
    );
    let pool = PgPool::connect(&database_url)
        .await
        .map_err(|e| anyhow::anyhow!("Failed to connect PostgreSQL test DB: {}", e))?;

    Ok(pool)
}
```


### プレゼンテーション層

#### `core/presentation/src/api/request.rs`

```rust
use serde::{
    Deserialize,
    Serialize,
};
use utoipa::ToSchema;

/// 問い合わせ作成リクエスト
#[derive(
    Debug,
    Clone,
    Serialize,
    Deserialize,
    PartialEq,
    Eq,
    ToSchema,
)]
pub struct CreateInquiryRequest {
    /// 氏名
    pub name: String,

    /// メールアドレス
    pub email: String,

    /// 問い合わせ内容
    pub message: String,
}

```

#### `core/presentation/src/api/response.rs`

```rust
use serde::{
    Deserialize,
    Serialize,
};
use utoipa::ToSchema;
use uuid::Uuid;

/// 問い合わせ
#[derive(
    Debug,
    Clone,
    Serialize,
    Deserialize,
    PartialEq,
    Eq,
    ToSchema,
)]
pub struct InquiryResponse {
    pub id: Uuid,
    pub name: String,
    pub email: String,
    pub message: String,
}

/// 問い合わせ作成レスポンス
#[derive(
    Debug,
    Clone,
    Serialize,
    Deserialize,
    PartialEq,
    Eq,
    ToSchema,
)]
pub struct CreateInquiryResponse {
    pub id: Uuid,
}

/// エラーレスポンス
#[derive(
    Debug,
    Clone,
    Serialize,
    Deserialize,
    PartialEq,
    Eq,
    ToSchema,
)]
pub struct ErrorResponse {
    pub message: String,
}
```

#### `core/presentation/src/api/inquiry_paths.rs`

```rust
use super::{
    request::CreateInquiryRequest,
    response::{
        CreateInquiryResponse,
        ErrorResponse,
        InquiryResponse,
    },
};

#[utoipa::path(
    post,
    path = "/api/v1/inquiry",
    tag = "Inquiry",
    request_body = CreateInquiryRequest,
    responses(
        (
            status = 201,
            description = "問い合わせ作成成功",
            body = CreateInquiryResponse
        ),
        (
            status = 400,
            description = "入力エラー",
            body = ErrorResponse
        ),
        (
            status = 500,
            description = "内部エラー",
            body = ErrorResponse
        )
    )
)]
pub fn create_inquiry_doc() {}

#[utoipa::path(
    get,
    path = "/api/v1/inquiries",
    tag = "Inquiry",
    params(
    ),
    responses(
        (
            status = 501,
            description = "未実装",
            body = ErrorResponse
        ),
        (
            status = 404,
            description = "存在しない",
            body = ErrorResponse
        ),
        (
            status = 500,
            description = "内部エラー",
            body = ErrorResponse
        )
    )
)]
pub fn find_inquiry_doc() {}
```

#### `core/presentation/src/api/openapi.rs`

```rust
use utoipa::OpenApi;

use super::inquiry_paths::*;
use super::request::CreateInquiryRequest;
use super::response::{
    CreateInquiryResponse,
    ErrorResponse,
    InquiryResponse,
};

#[derive(OpenApi)]
#[openapi(
    info(
        title = "Inquiry API",
        version = "1.0.0",
        description = "問い合わせシステムAPI"
    ),
    paths(
        create_inquiry_doc,
        find_inquiry_doc,
    ),
    components(
        schemas(
            CreateInquiryRequest,
            CreateInquiryResponse,
            InquiryResponse,
            ErrorResponse,
        )
    ),
    tags(
        (
            name = "Inquiry",
            description = "問い合わせAPI"
        )
    )
)]
pub struct ApiDoc;
```

#### `core/presentation/src/lambda/lambda_executor.rs`

```rust
use lambda_runtime::{Error, LambdaEvent, run, service_fn};
use std::future::Future;

pub async fn lambda_executor<T, U, F, Fut>(invoke: F) -> Result<(), Error>
where
    T: serde::de::DeserializeOwned,
    U: serde::Serialize,
    F: FnMut(LambdaEvent<T>) -> Fut,
    Fut: Future<Output = Result<U, Error>>,
{
    run(service_fn(invoke)).await
}
```

#### `core/presentation/src/lambda/router.rs`

```rust
use aws_lambda_events::apigw::ApiGatewayV2httpRequest;

#[derive(
    Debug,
    Clone,
    Copy,
    PartialEq,
    Eq,
)]
pub enum Route {
    CreateInquiry,
    FindInquiry,
    NotFound,
    MethodNotAllowed,
}

pub fn route(
    request: &ApiGatewayV2httpRequest,
) -> Route {
    let path = request
        .raw_path
        .as_deref()
        .or(
            request
                .request_context
                .http
                .path
                .as_deref(),
        )
        .unwrap_or("");

    let method = request.request_context.http.method.as_str();

    match (method, path) {
        ("POST", "/api/v1/inquiry") => {
            Route::CreateInquiry
        }

        ("GET", "/api/v1/inquiries") => {
            Route::FindInquiry
        }

        ("POST", _) | ("GET", _) => {
            Route::NotFound
        }

        _ => Route::MethodNotAllowed,
    }
}

#[cfg(test)]
mod tests {
    use super::*;
    use rstest::*;

    #[tokio::test]
    async fn 何らかのテスト() {
    }
}
```

#### `core/presentation/src/lambda/inquiry_handler.rs`

```rust
use std::sync::Arc;

use application::inquiry::{
    create::CreateInquiryUsecase,
    dto::CreateInquiryInput,
};

use aws_lambda_events::{
    apigw::{
        ApiGatewayV2httpRequest,
        ApiGatewayV2httpResponse,
    },
    encodings::Body,
};

use lambda_runtime::{
    Error,
    LambdaEvent,
};

use serde::Serialize;

use crate::{
    api::{
        request::CreateInquiryRequest,
        response::{
            CreateInquiryResponse,
            ErrorResponse,
        },
    },
    lambda::router::{
        route,
        Route,
    },
};

use domain::inquiry::repository::InquiryRepository;

pub async fn handler(
    repository: Arc<dyn InquiryRepository>,
    event: LambdaEvent<ApiGatewayV2httpRequest>,
) -> Result<ApiGatewayV2httpResponse, Error> {
    let (request, _) = event.into_parts();

    match route(&request) {
        Route::CreateInquiry => {
            create_inquiry(
                repository,
                request,
            )
            .await
        }

        Route::FindInquiry => {
            json_response(
                501,
                &ErrorResponse {
                    message: "not implemented".into(),
                },
            )
        }

        Route::NotFound => {
            json_response(
                404,
                &ErrorResponse {
                    message: "not found".into(),
                },
            )
        }

        Route::MethodNotAllowed => {
            json_response(
                405,
                &ErrorResponse {
                    message:
                        "method not allowed".into(),
                },
            )
        }
    }
}

async fn create_inquiry(
    repository: Arc<dyn InquiryRepository>,
    request: ApiGatewayV2httpRequest,
) -> Result<ApiGatewayV2httpResponse, Error> {
    let body = request.body.unwrap_or_default();

    let req: CreateInquiryRequest =
        serde_json::from_str(&body)
            .map_err(Error::from)?;

    let usecase =
        CreateInquiryUsecase::new(repository);

    match usecase.execute(input).await {
        Ok(created) => json_response(201, &CreateInquiryResponse { id: created.id }),
        Err(CreateInquiryError::Domain(_)) => json_response(
            400,
            &ErrorResponse {
                message: "invalid request".into(),
            },
        ),
        Err(CreateInquiryError::Repository(InquiryRepositoryError::Duplicate)) => json_response(
            409,
            &ErrorResponse {
                message: "inquiry already exists".into(),
            },
        ),
        Err(_) => json_response(
            500,
            &ErrorResponse {
                message: "internal server error".into(),
            },
        ),
    }
}

fn json_response<T>(
    status_code: i64,
    payload: &T,
) -> Result<ApiGatewayV2httpResponse, Error>
where
    T: Serialize,
{
    let body =
        serde_json::to_string(payload)?;

    Ok(ApiGatewayV2httpResponse {
        status_code,
        body: Some(Body::Text(body)),
        headers: {
            let mut headers = HeaderMap::new();
            headers.insert("content-type", HeaderValue::from_static("application/json"));
            headers
        },
        ..Default::default()
    })
}

#[cfg(test)]
mod tests {
    use super::*;
    use rstest::*;
    use tokio::sync::OnceCell;
    use sea_orm::Database;
    use infrastructure::repository::inquiry::seaorm_inquiry_repository::SeaOrmInquiryRepository;

    // テスト環境全体で共有するリポジトリのコンテナ
    // tokio::sync::OnceCell を使うと、非同期クロージャをそのまま渡せるため非常に便利です
    static TEST_INQUIRY_REPOSITORY: OnceCell<Arc<dyn InquiryRepository>> = OnceCell::const_new();

    // テスト前の初期化処理
    #[fixture]
    async fn repository() -> Arc<dyn InquiryRepository> {
        TEST_INQUIRY_REPOSITORY
            .get_or_init(|| async {
                use infrastructure::db::{config::DatabaseConfig::PostgreSQL, connection::create_connection};
                // 非同期接続処理を OnceCell の中で非同期的に完了させる
                // ※実際の実装にあたっては初期化フローでのエラーハンドリングを厳密に行うこと
                create_connection(&PostgreSQL {
                    host: "localhost".into(),
                    port: 5432,
                    database: "postgres".into(),
                    username: "insertonly".into(),
                    password: "insertonly".into(),
                })
                let repository = SeaOrmInquiryRepository::new(db_connection);
                Arc::new(repository) as Arc<dyn InquiryRepository>
            })
            .await
            .clone() // Arcのクローンなので軽量
    }

    #[rstest]
    #[tokio::test]
    async fn 何らかのテスト(#[future(awt)] repository: Arc<dyn InquiryRepository>) {
        // 何らかのテストを実装する
    }
}
```
